{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "fibonacci",
            "type": "shell",
            "command": "gcc -g -o Fib_heap Fib_heap.c -lm",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}